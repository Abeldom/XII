#include <iostream>
#include <fstream>

using namespace std;

ifstream in("input.txt");

struct csomo{
    int ertek;
    csomo* elod;
    csomo* bal;
    csomo* jobb;
};


void letrehoz(elek* index,int ertek){
    node[ertek].elod=index;
    node[ertek].bal==NULL;
    node[ertek].jobb=NULL;

    /*node* csomo=new node;
    csomo->index=index;
    csomo->bal=NULL;
    csomo->jobb=NULL;
    return csomo;
    */
};


int bejaras(csomo* node,1){
    if(node[valaki].bal!=0){

    }



}


/// 1.feladat
int main()
{
    int n;
    in>>n;

    csomo node[n+1];
    node[1].elod=NULL;

    for(int i=1;i<=n;i++){
        int jobbszomszed,balszomszed;
        in>>balszomszed>>jobbszomszed;
        if(balszomszed!=0){
            letrehoz(*node[i],balszomszed);
            node[i].bal=*node[balszomszed];
        }
        if(jobbszomszed!=0){
            letrehoz(*node[i],jobbszomszed);
            node[i].bal=*node[jobbszomszed];
        }
    }

    bejaras(*node[1],1);

  /*
    int J[n],B[n];
    for(int i=0;i<n;i++){
        in>>J[i];
    }
    for(int i=0;i<n;i++){
        in>>B[i];
    }
    int gyerek;
    cin>>gyerek;

    for(int i=0;i<n;i++){
        if(J[i]==gyerek || B[i]==gyerek){
            cout<<i+1;
            return 0;
        }
    }
    cout<<"nincs neki szulo";
*/
        return 0;
}
